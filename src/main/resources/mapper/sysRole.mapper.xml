<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.rsd.mapper.ISysRoleMapper">
    <select id="getRoleById" parameterType="int" resultType="org.rsd.bean.SysRole">
        select * from bu_sys_role where id=#{id}
    </select>
</mapper>